<div class="container">
<p id="notice"><%= notice %></p>

<h1>List of Movies</h1>

<table class="table">
  <thead>
    <tr>
      <!--<th>Movie</th>-->
      <th>Title</th>
      <!--<th>Cast</th>-->
      <!--<th>Crew</th>-->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @movies_infos.each do |movies_info| %>
      <tr>
        <!--<td><%= movies_info.movie_id %></td>-->
        <td><%= link_to movies_info.title, movies_info %></td>
        <%
=begin%>
 <td><%= movies_info.title %></td> 
<%
=end%>
        <!--<td><%= movies_info.cast %></td>-->
        <!--<td><%= movies_info.crew %></td>-->
        <td><%= link_to '✏', edit_movies_info_path(movies_info) %></td>
        <td><%= link_to '❌', movies_info, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate @movies_info, 
    list_classes: %w(pagination justify-content-center), 
    inner_window: 1, outer_window: 1, previous_label: '&lt;', 
    next_label: '&gt;'%>

<br>

<%= link_to 'Add a new movie', new_movies_info_path %>
</div>